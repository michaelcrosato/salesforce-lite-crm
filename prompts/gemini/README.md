# Gemini Prompts

Store Gemini-specific prompts here.

Prompts should name the branch, worktree path, tests/e2e/scripts scope, gate
commands, and report files. They should not override `PLAN.md` unless they
explicitly define a one-run exception.
