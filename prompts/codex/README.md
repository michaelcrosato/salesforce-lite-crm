# Codex Prompts

Store Codex-specific prompts here.

Prompts should name the branch, worktree path, allowed files, gate commands,
and report files. They should not claim features passed before the gate runs.
Any one-run exception to `PLAN.md` must be explicit in the prompt.
