# Claude Prompts

Store Claude-specific prompts here.

Prompts should name the branch, worktree path, UI/app-router scope, e2e
expectations, gate commands, and report files. They should not override
`PLAN.md` unless they explicitly define a one-run exception.
